---
execution_id: 2026_08_07_03_11_35_WI_ANNOTATE_0050
prompt_id: PROMPT(WI-ANNOTATE-0050:WI_ANNOTATE_0050)[2026-08-07T02:54:27+00:00]
work_item: WI-ANNOTATE-0050
status: landed
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/236
commit: 8063829842de0b9d911176888f9097714f28bee5
created_at: 2026-08-07T03:11:35+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-ANNOTATE-0050.md
session_transcript: claude-app:d95251cd-5bda-40d3-a06e-d330bc6e2921
---

# Summary

Implemented WI-ANNOTATE-0050: fix `assess_story`'s hardcoded
`max_tokens=2048` and `make_segment_extractor`'s silent inheritance of
`JSONPromptExtractor`'s bare 4096 default, both of which truncated on
real corpus stories and would block `lcats annotate` (WI-ANNOTATE-0051)
from running.

# Result

- `assess.py`: `assess_story()` gained a `max_tokens: int = 4096`
  parameter, threaded to its `backend.complete(...)` call (previously
  hardcoded `2048`).
- `assess_cli.py`: added a `--max-tokens` CLI flag (default 4096,
  validated `> 0`, mirroring the existing `--max-body-chars` pattern),
  wired through to `assess_story`.
- `scene_analysis.py`: `make_segment_extractor()` gained a
  `max_tokens: int = 16384` parameter (see real-API dogfood finding
  below — 8192 was tried first and proved insufficient), passed to
  `JSONPromptExtractor`'s constructor. All 3 existing call sites
  (`run_pilot.py`, `story_processors.py`,
  `experimental/model_comparison/`) get the raised default
  automatically — none needed changes.
- Extended `assess_test.py` (2 new tests: default raised to 4096 and
  overridable, both asserting against `FakeBackend.calls`'s real
  recorded kwargs) and `scene_analysis_test.py` (2 new tests: default
  raised to 16384 and overridable, asserting `extractor.max_tokens`
  directly).
- Discovered and fixed an environment issue before trusting any test
  run: the editable `lcats` install was pointing at a different
  worktree (`lcats-special-char-strategy-c18aa7`) via
  `module.__file__`; ran `scripts/develop` to repoint it at this
  worktree — confirmed via `python -c "from lcats.analysis.corpus
  import assess; print(assess.__file__)"` before re-running tests.
- Ran a pre-push `/lrh-self-review` diff-mode pass (cold `general-purpose`
  subagent, no session memory, given only the diff and work-item
  orientation) — clean, no findings; independently re-verified its two
  key claims (`story_processors.py:76`'s positional call, and
  `llm_extractor.py`'s `self.max_tokens` actually reaching
  `backend.complete`) myself before accepting the result. Separate
  execution record: `2026_08_07_03_09_56_WI_ANNOTATE_0050_SELFREVIEW`.
- Used `origin/main` rather than local `main` for the self-review diff
  and throughout, since this shared multi-worktree `.git`'s local
  `main` ref was stale (`05606ce7` vs. `origin/main`'s current tip) —
  a literal `git diff main` would have pulled in ~120 unrelated files
  from other already-merged PRs.

**Real-API dogfood run (user-requested follow-on, after unit-test
coverage above landed):** found `.secrets/` at the actual repo root
(`/Users/centaur/Workspace/LCATS/LCATS/.secrets`, outside this worktree
— worktrees don't inherit it) and pointed `load_secrets()` at it
explicitly. Ran 2 real calls against the corpus's longest real story
(`corpora/lovecraft/the_case_of_charles_dexter_ward/story.json`, 264KB,
~93K input tokens):

1. `assess_story` at the new `max_tokens=4096` default: succeeded
   cleanly (verdict=review, detected_genre=horror, 782 output tokens,
   no truncation). Had to patch a local, script-only copy of
   `ASSESSMENT_TOOL` to strip `detected_genre_confidence`'s
   `minimum`/`maximum` constraints first — a real, pre-existing,
   currently-blocking bug (Anthropic's strict tool-schema mode rejects
   min/max on `number` properties; introduced 2026-07-29 per `git
   blame`, unrelated to this work item) that would otherwise fail
   every real `lcats assess` call with a 400. Flagged as a separate
   follow-up task (not fixed here — out of this WI's scope), not
   silently routed around in the real `assess.py`.
2. `make_segment_extractor` at the first-tried `max_tokens=8192`:
   **truncated** — `stop_reason: max_tokens` at exactly 8192, with
   93,203 input tokens. This is real, decision-relevant evidence that
   8192 wasn't actually sufficient for the corpus's most extreme case.
   Raised to `16384` (matching `run_pilot.py`'s `_ERW_MAX_TOKENS`
   precedent for the identical failure mode) and re-ran against the
   same story: succeeded cleanly, 24 segments extracted, no truncation.

This is real evidence the shipped defaults (4096 for assess, 16384 for
segmentation) hold against the single longest, most extreme story in
the corpus — not just that the override parameter threads through
correctly.

# Validation

- `scripts/version tools` — ruff 0.15.0, black 25.11.0, Python 3.11.8.
- `scripts/format --check --diff` — clean.
- `scripts/lint` — clean.
- `scripts/test` — 1606 tests pass (full suite, re-run after the
  post-dogfood 16384 change; also after fixing the editable-install
  pointer above).
- `lrh validate` — 0 errors.
- Real-API dogfood run (2 live calls: 1 Anthropic assess, 1 Anthropic
  segmentation at the raised 16384) against the corpus's longest real
  story — both succeeded cleanly, no truncation. Acceptance criterion
  "verified against at least one real story that previously truncated"
  now fully met for `make_segment_extractor` (directly observed the
  8192→16384 truncation-to-success transition); `assess_story` verified
  clean at 4096 but the pre-existing schema bug (see below) meant this
  session never observed an actual 2048-ceiling truncation on this
  story to compare against — the 4096 default is evidenced as
  sufficient, not proven better than 2048 specifically for this story.

# Follow-up

- **New bug found, not fixed here (out of scope):**
  `ASSESSMENT_TOOL`'s `detected_genre_confidence` field
  (`assess.py` ~line 86) has `minimum`/`maximum` constraints Anthropic's
  strict tool-schema mode rejects — blocks every real `lcats assess`
  call today, introduced 2026-07-29, unrelated to this WI. Flagged as a
  spawned follow-up task this session (task_40c0970c).
- WI-ANNOTATE-0051 (`lcats annotate`) can now proceed — its dependency
  on this item's fixes is satisfied, and both fixes have real-API
  evidence behind them, not just unit-test coverage.
