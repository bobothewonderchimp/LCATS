---
execution_id: 2026_08_07_03_11_35_WI_ANNOTATE_0050
prompt_id: PROMPT(WI-ANNOTATE-0050:WI_ANNOTATE_0050)[2026-08-07T02:54:27+00:00]
work_item: WI-ANNOTATE-0050
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/236
commit: 9c7c088c055f86a7787340596b8611e354784b18
created_at: 2026-08-07T03:11:35+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-ANNOTATE-0050.md
session_transcript: claude-app:d95251cd-5bda-40d3-a06e-d330bc6e2921
---

# Summary

Implemented WI-ANNOTATE-0050: fix `assess_story`'s hardcoded
`max_tokens=2048` and `make_segment_extractor`'s silent inheritance of
`JSONPromptExtractor`'s bare 4096 default, both of which truncated on
real corpus stories and would block `lcats annotate` (WI-ANNOTATE-0051)
from running.

# Result

- `assess.py`: `assess_story()` gained a `max_tokens: int = 4096`
  parameter, threaded to its `backend.complete(...)` call (previously
  hardcoded `2048`).
- `assess_cli.py`: added a `--max-tokens` CLI flag (default 4096,
  validated `> 0`, mirroring the existing `--max-body-chars` pattern),
  wired through to `assess_story`.
- `scene_analysis.py`: `make_segment_extractor()` gained a
  `max_tokens: int = 8192` parameter, passed to
  `JSONPromptExtractor`'s constructor. All 3 existing call sites
  (`run_pilot.py`, `story_processors.py`,
  `experimental/model_comparison/`) get the raised default
  automatically — none needed changes.
- Extended `assess_test.py` (2 new tests: default raised to 4096 and
  overridable, both asserting against `FakeBackend.calls`'s real
  recorded kwargs) and `scene_analysis_test.py` (2 new tests: default
  raised to 8192 and overridable, asserting `extractor.max_tokens`
  directly).
- Discovered and fixed an environment issue before trusting any test
  run: the editable `lcats` install was pointing at a different
  worktree (`lcats-special-char-strategy-c18aa7`) via
  `module.__file__`; ran `scripts/develop` to repoint it at this
  worktree — confirmed via `python -c "from lcats.analysis.corpus
  import assess; print(assess.__file__)"` before re-running tests.
- Ran a pre-push `/lrh-self-review` diff-mode pass (cold `general-purpose`
  subagent, no session memory, given only the diff and work-item
  orientation) — clean, no findings; independently re-verified its two
  key claims (`story_processors.py:76`'s positional call, and
  `llm_extractor.py`'s `self.max_tokens` actually reaching
  `backend.complete`) myself before accepting the result. Separate
  execution record: `2026_08_07_03_09_56_WI_ANNOTATE_0050_SELFREVIEW`.
- Used `origin/main` rather than local `main` for the self-review diff
  and throughout, since this shared multi-worktree `.git`'s local
  `main` ref was stale (`05606ce7` vs. `origin/main`'s current tip) —
  a literal `git diff main` would have pulled in ~120 unrelated files
  from other already-merged PRs.

**Gap, disclosed rather than silently skipped:** the work item's
acceptance criteria calls for verifying both fixes against a real story
that previously truncated. No `ANTHROPIC_API_KEY` or `.secrets/` was
available in this session, so live-API verification wasn't possible —
only the override mechanism itself is verified (via unit tests
asserting the value reaches the real backend call/extractor instance),
not an actual truncation-to-success transition against a live model.

# Validation

- `scripts/version tools` — ruff 0.15.0, black 25.11.0, Python 3.11.8.
- `scripts/format --check --diff` — clean.
- `scripts/lint` — clean.
- `scripts/test` — 1606 tests pass (full suite, after fixing the
  editable-install pointer above).
- `lrh validate` — 0 errors.

# Follow-up

- Real-story truncation verification (acceptance criterion not fully
  met — see Gap above) — needs a session with `ANTHROPIC_API_KEY`
  available, or an explicit decision that unit-test coverage of the
  override mechanism is sufficient evidence for this item.
- WI-ANNOTATE-0051 (`lcats annotate`) can now proceed — its dependency
  on this item's fixes is satisfied.
