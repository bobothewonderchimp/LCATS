---
execution_id: 2026_08_08_02_33_22_WI_ANNOTATE_0054
prompt_id: PROMPT(WI-ANNOTATE-0054:WI_ANNOTATE_0054)[2026-08-07T22:55:06+00:00]
work_item: WI-ANNOTATE-0054
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/253
commit: b5f8032d7663e46811aab72e4032d384914ba2bf
created_at: 2026-08-08T02:33:22+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-ANNOTATE-0054.md
session_transcript: claude-app:d95251cd-5bda-40d3-a06e-d330bc6e2921
---

# Summary

Ran `lcats annotate`'s real pipeline over a hand-picked 24-story subset
(3 stories x 8 current `VALID_GENRES`, no paid API call used for
selection), validated the output, and produced a per-genre stats
report — the final item in `WS-WORLDCON-FAST-PATH-ANNOTATION`.

# Result

- `select_subset.py`: hand-picked 24 stories from `corpora/` by title/
  author/opening-text judgment (documented rationale per story in
  `subset_manifest.md`), copied into
  `lcats/experimental/annotation_feasibility_trial/source/trial/`.
- Real API run: `claude-opus-4-8`, 24 stories, 17m39s wall time. All 24
  produced `genre.json`/`scenes.json`/`README.md`.
- Validated via `lcats promote`'s formal `survey_collection` gate (0
  mojibake findings, 0 malformed-sidecar findings across all 24
  stories) plus manual inspection of a sample spanning multiple genres
  (checked full `genre.json`/`scenes.json`/`README.md` content, not
  just exit codes).
- `collect_stats.py` → `stats_report.md`: 23/24 provisional/detected
  genre agreement, average confidence 0.88 (range 0.55-0.99), average
  scene count 7.4 per story.
- **Data quality finding**: 10/24 stories (42%) have a corrupted
  `secondary_genre` value — leaked tool-call-syntax fragments (e.g.
  `</antml="secondary_genre">`) instead of a genre tag. Traced during
  hand validation: confirmed not a parsing bug in `lcats`'s own code
  (`anthropic_backend.py` reads the Anthropic SDK's already-parsed
  native `tool_use.input` dict) and not prompt injection from the story
  text or tool schema (`assess.py`'s schema/prompts contain no such
  tags). Scoped entirely to `secondary_genre`; every other field
  (`detected_genre`, `summary`, `issues`, scene segmentation) is
  unaffected. Full detail and the affected-story list are in
  `stats_report.md`'s "Data quality finding" section. Recommended a
  follow-up work item for output sanitization — not implemented here,
  out of this evaluation-only item's scope.
- Also noted (not fixed, out of scope): `scene_analysis.py`'s
  segmentation call does not record token usage the way `assess.py`'s
  genre call does, so this item's cost-visibility goal (Required
  Changes #5) is only partially satisfiable from current sidecar data.
- Ran a pre-push `/lrh-self-review` diff-mode pass (cold subagent, full
  diff + WI orientation) — recomputed every stats_report.md statistic
  independently from the raw JSON and confirmed a match; found one
  minor latent gap (the Mismatches section didn't route through the
  same corrupted-value sanitization the per-story table used, harmless
  in this run's actual data but a real bug for any future rerun) —
  independently re-verified this claim myself (read the actual code)
  before fixing it.

# Validation

- `scripts/version tools` — ruff 0.15.0, black 25.11.0.
- `scripts/format --check --diff`, `scripts/lint` — clean (note:
  `lcats/experimental/` is excluded from `scripts/format`'s scope by
  project convention, per `AGENTS.md`).
- `scripts/test` — 1652 tests pass (full suite).
- `lrh validate` — 0 errors.
- Editable-install-pointing-at-a-different-worktree issue recurred and
  was fixed 3 more times this session (concurrent sessions' own
  `scripts/develop` runs) — verified `module.__file__` before trusting
  each test/CLI run.
- `lcats promote`'s `survey_collection` run directly against the trial
  output: `clean=True`, 0 findings of either kind, `story_count=24`.
- Manual review of `genre.json`/`scenes.json`/`README.md` content for a
  sample of stories across multiple genres (horror, fantasy, adventure,
  western) — not just a clean exit code.

# Follow-up

- Recommend a new work item: sanitize/validate free-text tool-result
  fields in `assess.py`'s genre-detection call (the `secondary_genre`
  corruption above) — e.g. detect and strip or reject responses
  containing leaked tool-call-syntax patterns, matching how
  `_annotate_scenes` already rejects malformed segmentation output.
- Recommend a follow-up to add token-usage recording to
  `scene_analysis.py`'s `make_segment_extractor`, matching `assess.py`'s
  existing `input_tokens`/`output_tokens` fields, to fully satisfy this
  item's cost-visibility goal for any future larger run.
- This PR needs the standard review-response → confirm-fixes → merge →
  closeout cycle (`/lrh-land`, inlined by `/lrh-execute`'s own Step 4)
  before WI-ANNOTATE-0054 resolves. Per standing policy: only the
  automatic first-push bot review, no manual retrigger — further rounds
  substitute `/lrh-self-review` PR-mode.
