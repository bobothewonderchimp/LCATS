---
execution_id: 2026_08_07_18_00_26_WI_LLM_0049
prompt_id: PROMPT(WI-LLM-0049:WI_LLM_0049)[2026-08-07T16:27:10+00:00]
work_item: WI-LLM-0049
status: landed
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/245
commit: 6b806efec93695f4331f858f06223616ff1fe2e6
created_at: 2026-08-07T18:00:26+00:00
agent: claude_app
instruction_source: project/work_items/proposed/WI-LLM-0049.md
session_transcript: claude-app:6d988910-ee4a-4ccc-af0b-2fb13d91ddc5
---

# Summary

Implement WI-LLM-0049 via `/lrh-execute` (which inlines `/lrh-implement`
then `/lrh-land`) - add a `qwen3:30b-a3b` (MoE) local-model candidate to
`lcats/experimental/model_comparison/`, testing whether it narrows
`qwen3:8b`'s entity-recall gap against `claude-opus-4-8`.

# Result

Pulled `qwen3:30b-a3b` via Ollama (confirmed `temperature=0.6/top_k=20/
top_p=0.95` via `ollama show --parameters`, matching Qwen3's own
documented recommendation and `qwen3:8b`'s existing setting). Added
`ollama_qwen3_30b_a3b/` (`README.md`, `setup.py`, `benchmark.py`,
mirroring `ollama_qwen3_8b/`'s shape) and ran `benchmark.py` 3 times
(exceeding the ≥2 acceptance criterion), committing each as
`results_run1-3.json`.

**The hypothesis was NOT supported.** Run 1: success, 192.1s, 1 entity.
Run 2: truncated failure at the `max_tokens=8192` ceiling, 218.1s. Run 3:
success, 148.2s, 1 entity. `qwen3:30b-a3b` proved both slower (148-218s
vs. `qwen3:8b`'s 74-106s) and markedly less reliable (2/3 runs
structurally succeeded but returned essentially empty results) than the
smaller dense model it was meant to outperform. An uncommitted diagnostic
call (outside `benchmark.py`'s normal path, done to understand the
pattern) showed one such run calling `extract_entities` with a single
`segment` field echoing the input text back verbatim rather than
populating the required `entities` array - a genuine, real anomaly, not
random noise (2 of 3 runs showed this exact shape). A separate ad-hoc
diagnostic call did succeed with a correct 13+-entity list, confirming
the model *can* do the task, just inconsistently. Root cause not
conclusively diagnosed - documented as an open question, cross-referenced
to `WI-LLM-0051`'s adjacent `tool_choice`-reliability investigation.

Updated the top-level `model_comparison/README.md` and
`PROP-ERW-LOCAL-MODEL-EVALUATION`'s landscape section with this finding
(the proposal's original text framed `30b-a3b` as an untested "plausible"
quality-tier candidate; now corrected).

Ran a diff-mode `/lrh-self-review` pass before the PR's first push
(execution record: `2026_08_07_17_57_25_WI_LLM_0049_IMPL_SELFREVIEW.md`)
per the user's explicit instruction to use self-review rather than
GitHub bots for this run, and per this session's newly-established
standing guidance to never manually retrigger Codex/Copilot (limited
monthly quota, 6/7 already consumed). The subagent verified all numeric
claims trace to committed JSON via a live `benchmark_summary.py` re-run
and surfaced one real caveat (the entity-count-1 diagnosis rests on an
uncommitted call, not recoverable from `results.json` alone), which was
independently re-verified (confirmed `raw_output_preview` is `None` by
construction on any structurally-successful tool call -
`common/harness.py:197`, `llm_extractor.py:349`) and fixed by documenting
the limitation and cross-referencing `WI-LLM-0055`. Caught and corrected
a real process error along the way: the self-review fix was initially
edited into the working tree but never re-staged before committing, so
the first implementation commit silently shipped without it despite the
commit message describing it as included - added as an explicit
follow-up commit once noticed via a post-commit `git status` check.

# Validation

- `scripts/version tools` - `ruff 0.15.0`, `black 25.11.0` (CI-pinned
  versions).
- `scripts/format --check --diff` - clean.
- `scripts/lint` - clean.
- `scripts/test` - 1632 tests, `OK` (after fixing a stale editable
  install pointing at a different worktree - a recurring issue this
  session, resolved via `pip install -e .`).
- `lrh validate` - 0 errors, pre-existing warnings only.
- 3 real `benchmark.py` runs (not simulated), all 3 committed as
  evidence regardless of outcome (including the 1 genuine failure).

# Follow-up

- Consider filing a follow-on item specifically for the "structurally
  succeeds but returns near-empty/malformed results" failure mode if it
  recurs with other candidates - distinct from `WI-LLM-0051`'s
  "tool never called at all" investigation.
- `WI-LLM-0055` (full entity-list capture) would also close the
  evidence-quality gap this run surfaced: `results.json` currently can't
  distinguish "the model returned 1 real entity" from "the model
  returned 1 malformed field" without an out-of-band diagnostic call.
