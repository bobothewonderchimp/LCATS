---
execution_id: 2026_08_07_16_13_12_WI_ANNOTATE_0051_REVIEW
prompt_id: PROMPT(AD_HOC:WI_ANNOTATE_0051_REVIEW)[2026-08-07T16:13:00+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 2026_08_07_07_01_05_WI_ANNOTATE_0051
pr: https://github.com/xenotaur/LCATS/pull/241
commit: 3aef233e0c91416c9ea4639a001aa1635add53ea
created_at: 2026-08-07T16:13:12+00:00
agent: claude_app
instruction_source: https://github.com/xenotaur/LCATS/pull/241
session_transcript: claude-app:d95251cd-5bda-40d3-a06e-d330bc6e2921
---

# Summary

Address open review comments on PR #241, fetched via GraphQL directly
(`lrh request review_response` reported "Nothing to resolve" despite 6
real, unresolved, non-outdated threads existing — a discrepancy worth
noting, not yet root-caused).

# Result

6 comments from `chatgpt-codex-connector` (5 P1, 1 P2), all triaged as
present/valid/feasible and fixed, each independently re-verified against
the actual code before fixing:

1. (P1) **Alignment/validation errors not rejected.** Verified
   `llm_extractor.py`'s `extract()`: on an `alignment_error` or
   `validation_error` exception, `extracted_output` is still set to the
   un-aligned `parsed` value (truthy), and `_annotate_scenes` only
   checked `api_error`/`extraction_error`. Fixed by also checking
   `alignment_error`/`validation_error`.
2. (P1) **Stale sidecars survive a failed recompute.** Confirmed a
   failed genre/scenes recompute left the prior sidecar file on disk
   unchanged while the README/other sidecar could still update, mixing
   configurations. Fixed: on a genuine failure (not a cache hit), the
   corresponding sidecar file is now removed. Added a regression test
   (`test_stale_genre_sidecar_removed_when_recompute_fails`).
3. (P1) **Non-atomic sidecar writes.** `_write_json` used plain
   `write_text`, which can leave torn JSON if interrupted mid-write even
   after the checkpoint already recorded success. Fixed with a
   tempfile+`os.replace` pattern mirroring `checkpoint.write_checkpoint`'s
   own.
4. (P1) **Member imports violate AGENTS.md.** Verified
   `AGENTS.md:26-30`'s explicit module-import rule; confirmed
   `promote.py`'s own precedent (`from lcats.analysis.corpus import
   cli`). Fixed by switching to a module import and qualifying all
   calls.
5. (P2) **Genre fingerprint missing author/url.** Confirmed
   `assess_story` sends title/author/url to the model and stores them
   in `AssessmentResult`. Added `author`/`url` to the fingerprint;
   deliberately excluded `title` since `infer_story_title` derives it
   purely from the bucket directory name, already encoded in `item_id`.
6. (P2) **Scene fingerprint missing user prompt template.** Confirmed
   `SCENE_SEQUEL_USER_PROMPT_TEMPLATE` is the actual user message sent
   to the model, not just the system prompt. Added its hash to the
   fingerprint.

No exceptions (Unaddressed/Partial/Ambiguous/Problematic) — all 6
comments resolved by the above fixes.

# Validation

- `scripts/format --check --diff`, `scripts/lint`, full `scripts/test`
  (1619 tests, 1 new regression test), `lrh validate` (0 errors) — all
  re-run clean after applying every fix.
- Tool-version drift recurred twice more during this round (concurrent
  sessions bumping the shared conda env's black/ruff) — reinstalled
  pinned versions each time rather than reformatting to match drift.
- Editable install recurred pointing at a different worktree once more
  mid-round — re-verified via `module.__file__` and re-ran
  `scripts/develop` before trusting any test run.

# Follow-up

- The `lrh request review_response` vs. GraphQL discrepancy (CLI
  reported clean with 6 real unresolved non-outdated threads present)
  is worth investigating in a future session — not root-caused here,
  worked around by querying GraphQL directly.
