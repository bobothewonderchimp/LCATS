---
execution_id: 2026_08_26_03_31_18_WI_EVENT_0096
prompt_id: PROMPT(AD_HOC:WI_EVENT_0096)[2026-08-26T03:29:23+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: https://github.com/xenotaur/LCATS/pull/396
commit: 7f67db57
agent: claude_app
instruction_source: project/work_items/proposed/WI-EVENT-0096.md
session_transcript: pending
created_at: 2026-08-26T03:31:18+00:00
---

# Summary

Created work item `WI-EVENT-0096`: a real-API measurement item that closes
`WI-EVENT-0033`'s one outstanding acceptance criterion - a live re-run to
measure whether its schema-hardening fix (PR #188) actually reduced the
65% (11/17) `parsing_error` segmentation-exclusion rate observed before
that fix. Requested via `/lrh-work-item` with the args "to accomplish this
final scope of work[,] the results of this run must be saved as data
somewhere (perhaps in the experiments directory if it's large enough, in
the experimental directory if not)" - a direct continuation of this
session's own prior discussion identifying WI-EVENT-0033's unmet criterion.

# Result

Created `project/work_items/proposed/WI-EVENT-0096.md` (type: evaluation,
`depends_on: [WI-EVENT-0033]`, related workstream
`WS-EVENT-STRUCTURED-OUTPUT-RELIABILITY`). Confirmed via research that the
original 17-story sample's exact story IDs were never committed to the
repo (no `pilot_stories.jsonl` or equivalent artifact found), so the WI
scopes a fresh equivalent stratified sample instead, explicitly including
`western` (the stratum with zero included stories in the original
measurement) - matching WI-EVENT-0033's own acceptance-criteria wording
("or an equivalent smoke sample"). Per the user's explicit instruction,
the WI requires raw results + summary to be saved as committed data,
scoped to `lcats/experimental/segmentation_schema_verification/` (not the
top-level `experiments/` directory) since this is a small, single-call-
per-story sample rather than a full pilot-scale run - both directories
were confirmed to exist in this repo before choosing between them.
Opened PR #396 (branch `xenotaur/audit/wi-event-0096`, commit `7f67db57`).

# Validation

- `lrh validate` - 0 errors, 239 warnings (pre-existing baseline)

# Follow-up

- `session_transcript: pending` above should be updated to the durable
  session pointer once available.
- This item does not itself implement the measurement - `/lrh-execute
  WI-EVENT-0096` (or manual execution) is the next step, gated on
  `WI-EVENT-0033` per `depends_on` (already merged, so not a blocker) and
  on presenting a real-API cost estimate for explicit approval before any
  spend, per this item's own acceptance criteria.
- No workstream frontmatter update was made in this pass -
  `WS-EVENT-STRUCTURED-OUTPUT-RELIABILITY.md`'s `work_items:` list
  currently lists only `WI-EVENT-0032`/`WI-EVENT-0033`/`WI-EVENT-0061`;
  offering to add `WI-EVENT-0096` to it is a candidate follow-up, not yet
  actioned pending user confirmation.
