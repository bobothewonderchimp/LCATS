---
execution_id: 2026_08_07_19_40_00_WI_ANNOTATE_0052_REVIEW
prompt_id: PROMPT(AD_HOC:WI_ANNOTATE_0052_REVIEW)[2026-08-07T19:39:49+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 2026_08_07_19_30_01_WI_ANNOTATE_0052
pr: https://github.com/xenotaur/LCATS/pull/248
commit: 7083ff001269032ef391933735106f16e6450df9
created_at: 2026-08-07T19:40:00+00:00
agent: claude_app
instruction_source: https://github.com/xenotaur/LCATS/pull/248
session_transcript: claude-app:d95251cd-5bda-40d3-a06e-d330bc6e2921
---

# Summary

Address the automatic first-push bot review findings on PR #248 (the
only bot review permitted per standing user policy — no manual
retrigger).

# Result

2 comments (1 codex P1, 1 copilot), both triaged as present/valid/
feasible and fixed:

1. (codex, P1) **Required-key value types not validated.**
   `_validate_sidecars` checked `required_key not in data` (presence
   only), so `{"segments": null}` or `{"detected_genre": 42}` would
   pass even though the real writer (`annotate.py`) never emits those
   shapes. Verified against the actual code before fixing. Fixed by
   extending `_SIDECAR_REQUIRED_KEYS` to a 3-tuple (filename, key,
   expected type) and adding an `isinstance` check after the
   presence check.
2. (copilot) **`promote.py` importing `annotate` pulls in the full
   LLM/extractor dependency chain** just for two filename constants.
   Verified by direct measurement:
   `python3 -c "import time; t0=time.time(); import
   lcats.analysis.corpus.promote; print(time.time()-t0)"` showed
   1.825s before the fix. Fixed by moving `GENRE_SIDECAR_FILENAME`/
   `SCENES_SIDECAR_FILENAME` to `discovery.py` (already the shared
   home for canonical story-bucket filename conventions, alongside
   `CANONICAL_STORY_FILENAME`) — `promote.py` now imports from
   `discovery` (already imported), not `annotate`. `annotate.py` keeps
   both names as aliases pointing at `discovery`'s constants for
   backward compatibility. Re-measured after the fix: 0.823s — over
   1 second saved, confirming the finding was real, not just
   theoretical.

No exceptions (Unaddressed/Partial/Ambiguous/Problematic) — both
findings fully resolved by the above fixes.

# Validation

- `scripts/version tools` — caught and fixed another tool-version drift
  (black 26.3.1 vs pinned 25.11.0) before validating.
- `scripts/format --check --diff`, `scripts/lint` — clean.
- Editable install re-verified via `module.__file__` before trusting
  test output.
- `scripts/test tests/analysis_tests/promote_test.py` — 29 tests pass
  (2 new: `test_null_required_value_sidecar_blocks`,
  `test_wrong_value_type_sidecar_blocks`).
- `scripts/test` (full suite) — 1646 tests pass.
- `lrh validate` — 0 errors.
- Direct before/after import-time measurement (see above) confirming
  the copilot finding's real-world impact, not just accepting the
  claim at face value.

# Follow-up

None — both findings fully resolved in this diff.
