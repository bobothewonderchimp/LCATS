---
execution_id: 2026_08_29_07_44_27_WI_SEGMENT_0101
prompt_id: PROMPT(AD_HOC:WI_SEGMENT_0101)[2026-08-29T06:38:21+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 
pr: 
commit: 
agent: claude_app
instruction_source: "user request in-session (WI-SEGMENT-0098 paragraph-boundary root-cause discussion)"
session_transcript: pending
created_at: 2026-08-29T07:44:27+00:00
---

# Summary

Created work item `WI-SEGMENT-0101`: investigate whether rewording the segmentation prompt's `end_par_id`/
`start_par_id` instructions to be explicitly derived from
`end_exact`/`start_exact`'s located paragraph - rather than independently
judged - reduces or eliminates the one-directional paragraph-boundary
overshoot pattern `WI-SEGMENT-0098` found in 6/10 real `WI-EVENT-0096`
failures.

# Result

Direct inspection of the production prompt
(`lcats/src/lcats/analysis/scene_analysis.py:99-105,132-143,189-220`)
found that `end_par_id`/`start_par_id` and `end_exact`/`start_exact` are
documented as two independently-generated, redundant "location
selectors" with no instruction or schema constraint requiring them to
agree - a gap `WI-SEGMENT-0098`'s own conclusion (prompt fix not viable)
did not check against the actual prompt text before ruling it out. Filed
`WI-SEGMENT-0101` to test a targeted reword (derive `end_par_id` from
`end_exact`'s location) via a real-API ablation, on the same branch as
the companion `WI-SEGMENT-0102` (Option 4 fuzzy-matcher regression-safety
check) per explicit user instruction to keep both in one review cycle.
Wrote `lcats/project/work_items/proposed/WI-SEGMENT-0101.md`. PR not yet
opened - held per explicit user instruction until both work items in
this branch are ready together.

# Validation

- `lrh validate` - 0 errors (285 warnings, pre-existing baseline plus the
  standard `unassigned`-owner warnings both new WIs carry)

# Follow-up

- Push the branch and open the PR once `WI-SEGMENT-0102` is also written
  and committed (explicit user instruction: single branch, single review
  cycle, not yet pushed).
- `session_transcript` still `pending` - update to the durable session
  pointer before landing.
