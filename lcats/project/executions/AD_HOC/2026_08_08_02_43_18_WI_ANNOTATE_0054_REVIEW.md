---
execution_id: 2026_08_08_02_43_18_WI_ANNOTATE_0054_REVIEW
prompt_id: PROMPT(AD_HOC:WI_ANNOTATE_0054_REVIEW)[2026-08-08T02:43:11+00:00]
work_item: AD_HOC
status: in_progress
rerun_of: 2026_08_08_02_33_22_WI_ANNOTATE_0054
pr: https://github.com/xenotaur/LCATS/pull/253
commit: 001834dbdea001e80245df7d2f81014e5c0c0885
created_at: 2026-08-08T02:43:18+00:00
agent: claude_app
instruction_source: https://github.com/xenotaur/LCATS/pull/253
session_transcript: claude-app:d95251cd-5bda-40d3-a06e-d330bc6e2921
---

# Summary

Address the automatic first-push bot review findings on PR #253 (the
only bot review permitted per standing user policy — no manual
retrigger).

# Result

3 comments (all P2, `chatgpt-codex-connector`), triaged:

1. **Preserve annotation outputs when rebuilding the subset.**
   Present, valid, feasible — fixed. `select_subset.py`'s
   `build_subset()` did `shutil.rmtree(TRIAL_DIR)` on every run,
   destroying already-paid `genre.json`/`scenes.json`/`README.md`
   sidecars for every story still in the current selection if the
   script were ever rerun. Verified by reading the code, then
   confirmed the fix by actually rerunning `select_subset.py` against
   the already-annotated trial data: all 24 sidecars survived, `git
   status` showed zero diff. Now only removes subdirectories no longer
   present in `SELECTIONS`, and writes/overwrites only `story.json` in
   each selected story's own directory.
2. **Record the complete run cost before closing the evaluation.**
   Present, valid — partially addressed, not fully resolved for this
   specific run. Traced the root cause: `_annotate_scenes`
   (`annotate.py`) already receives token usage from
   `JSONPromptExtractor.extract()`'s own `"usage"` dict but was
   discarding it, so `scenes.json` never recorded
   `input_tokens`/`output_tokens` the way `genre.json` does. This was a
   small, contained fix entirely within `annotate.py` (no change to
   the shared `llm_extractor.py`/`scene_analysis.py` library code) —
   fixed, with a new test assertion (`annotate_test.py`). Every future
   run will have complete segmentation cost data. Deliberately did
   **not** re-run this trial's 24 already-good segmentation results
   solely to backfill token counts for this report — that would spend
   real API budget purely for reporting completeness, against this
   work item's own Risk Notes ("keep the subset genuinely small for
   this first run"). `stats_report.md` updated to explain both the fix
   and this scope decision transparently rather than silently
   omitting the gap.
3. **Add the required WI-ANNOTATE-0054 execution record.** Stale —
   already fixed before this bot review ran. The bot reviewed the
   first-push commit (`b5f8032d`); the execution record was added in a
   second commit (`81079927`) before the review comment posted.
   Verified: `find project/executions/WI-ANNOTATE-0054` and `gh pr view
   ... --json headRefOid` both confirm the record is present at the
   PR's current HEAD. No action needed.

No exceptions (Unaddressed/Ambiguous/Problematic) — all three findings
triaged with verified rationale.

# Validation

- Re-ran `select_subset.py` against the already-annotated trial output
  directly (not a synthetic test) to confirm the rmtree fix: all 24
  `genre.json`/`scenes.json`/`README.md` sidecars preserved, `git
  status` showed zero diff on `source/trial/`.
- `scripts/format --check --diff`, `scripts/lint` — clean.
- Editable-install-pointing-at-a-different-worktree issue recurred
  again and was fixed before trusting test output.
- `scripts/test tests/analysis_tests/annotate_test.py` — 14 tests pass
  (including the new `input_tokens`/`output_tokens` assertion).
- `scripts/test` (full suite) — 1652 tests pass.
- `lrh validate` — 0 errors.
- Re-ran `lcats promote`'s `survey_collection` directly against the
  trial output after the `annotate.py` schema change: still
  `clean=True`, 0 sidecar findings, `story_count=24` — confirms adding
  `input_tokens`/`output_tokens` to `scenes.json` didn't break the
  promote-gate validator's shape checks.

# Follow-up

None beyond what's already recorded in the primary execution record
(the two recommended follow-up work items: output sanitization for the
`secondary_genre` corruption, and this PR's own
`input_tokens`/`output_tokens` fix already closes the
`scene_analysis.py` cost-visibility gap that record separately
flagged).
