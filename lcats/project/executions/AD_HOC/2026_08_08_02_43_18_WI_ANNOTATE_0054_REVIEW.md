---
execution_id: 2026_08_08_02_43_18_WI_ANNOTATE_0054_REVIEW
prompt_id: PROMPT(AD_HOC:WI_ANNOTATE_0054_REVIEW)[2026-08-08T02:43:11+00:00]
work_item: AD_HOC
status: landed
rerun_of: 2026_08_08_02_33_22_WI_ANNOTATE_0054
pr: https://github.com/xenotaur/LCATS/pull/253
commit: d6ba49203f2c7952df37954bcfc0fb9415e9c399
created_at: 2026-08-08T02:43:18+00:00
agent: claude_app
instruction_source: https://github.com/xenotaur/LCATS/pull/253
session_transcript: claude-app:d95251cd-5bda-40d3-a06e-d330bc6e2921
---

# Summary

Address the automatic first-push bot review findings on PR #253 (the
only bot review permitted per standing user policy — no manual
retrigger).

# Result

6 comments total, from two bots on the same first-push review round
(`copilot-pull-request-reviewer` posted ~6 minutes after
`chatgpt-codex-connector`, still on the original push, before any fix
commit — both are the permitted automatic first-push review, not a
manual retrigger). Triaged:

**3 comments (all P2, `chatgpt-codex-connector`):**

1. **Preserve annotation outputs when rebuilding the subset.**
   Present, valid, feasible — fixed. `select_subset.py`'s
   `build_subset()` did `shutil.rmtree(TRIAL_DIR)` on every run,
   destroying already-paid `genre.json`/`scenes.json`/`README.md`
   sidecars for every story still in the current selection if the
   script were ever rerun. Verified by reading the code, then
   confirmed the fix by actually rerunning `select_subset.py` against
   the already-annotated trial data: all 24 sidecars survived, `git
   status` showed zero diff. Now only removes subdirectories no longer
   present in `SELECTIONS`, and writes/overwrites only `story.json` in
   each selected story's own directory.
2. **Record the complete run cost before closing the evaluation.**
   Present, valid — partially addressed, not fully resolved for this
   specific run. Traced the root cause: `_annotate_scenes`
   (`annotate.py`) already receives token usage from
   `JSONPromptExtractor.extract()`'s own `"usage"` dict but was
   discarding it, so `scenes.json` never recorded
   `input_tokens`/`output_tokens` the way `genre.json` does. This was a
   small, contained fix entirely within `annotate.py` (no change to
   the shared `llm_extractor.py`/`scene_analysis.py` library code) —
   fixed, with a new test assertion (`annotate_test.py`). Every future
   run will have complete segmentation cost data. Deliberately did
   **not** re-run this trial's 24 already-good segmentation results
   solely to backfill token counts for this report — that would spend
   real API budget purely for reporting completeness, against this
   work item's own Risk Notes ("keep the subset genuinely small for
   this first run"). `stats_report.md` updated to explain both the fix
   and this scope decision transparently rather than silently
   omitting the gap.
3. **Add the required WI-ANNOTATE-0054 execution record.** Stale —
   already fixed before this bot review ran. The bot reviewed the
   first-push commit (`b5f8032d`); the execution record was added in a
   second commit (`81079927`) before the review comment posted.
   Verified: `find project/executions/WI-ANNOTATE-0054` and `gh pr view
   ... --json headRefOid` both confirm the record is present at the
   PR's current HEAD. No action needed.

**3 comments (`copilot-pull-request-reviewer`), one each on
`source/trial/springtime_a_la_carte/scenes.json`,
`.../snow_white_and_rose_red/scenes.json`, `.../story_of_keesh/scenes.json`:**
flagged corrupted `start_exact`/`end_exact` anchor text (stray
`\u\u`/`\x0c`/quote-artifact fragments). Present, valid — investigated
well beyond the comments' own framing, and found this points at a more
serious underlying defect than "garbled anchor text":

- For `springtime_a_la_carte` and `snow_white_and_rose_red`, the
  garbled anchor text is genuinely cosmetic — the derived
  `start_char`/`end_char` offsets are still correct (verified against
  the real story text; the corruption is confined to the human-readable
  anchor string, likely the model mis-rendering a smart-quote/special
  character at that exact point).
- For `story_of_keesh`, the corruption is **not** cosmetic: the
  resulting `end_char` for segment 4 fell back to end-of-document,
  overlapping segment 5 by 2,626 characters — a real, wrong
  segmentation boundary.
- Pulling this thread further (checking every story programmatically
  for offset overlap and paragraph-index collapse, not just the 3
  stories the bot happened to flag) surfaced a **third, unflagged**
  story with the same defect in a worse form:
  `love_of_life` (Jack London) collapsed to one paragraph and produced
  8 overlapping segments (121k cumulative overlapping chars), and a
  **fourth**, `brown_wolf` (also Jack London), degenerated to a single
  segment spanning the entire 31.8k-char story — i.e. **all 3
  `corpora/london` stories in this trial (100%)** are affected by the
  same root cause, not just the ones the bot's pattern-matching
  happened to catch.
- Root cause fully traced to `text_segmenter.py`: `build_paragraph_index`
  only splits on a literal blank line; single-newline-formatted source
  text (all 3 `london` stories) collapses to `n_paragraphs=1`, forcing
  every segment's anchor search across the *entire document*, and
  `align_segment` silently falls back to end-of-document on any failed
  anchor match instead of raising an alignment error.
- **Not fixed here** — `text_segmenter.py`'s `align_segment`/
  `build_paragraph_index` are shared library code used by other
  callers (`story_processors.py`, `run_pilot.py`,
  `notebooks/12_extract_scenes.ipynb`); a correct fix needs its own
  design and test coverage against those callers, not a hasty patch
  mid-review-response for an evaluation-only work item. Documented as
  the report's lead finding (`stats_report.md`, above the
  `secondary_genre` section) with automated detection added to
  `collect_stats.py` (paragraph-count and offset-overlap checks) so
  the finding is measured, not asserted, and a dedicated follow-up
  work item is recommended.

No exceptions (Unaddressed/Ambiguous/Problematic) — all six findings
triaged with verified rationale; the copilot findings led to
independent further investigation beyond what was reported, not just a
literal fix of the 3 flagged instances.

# Validation

- Re-ran `select_subset.py` against the already-annotated trial output
  directly (not a synthetic test) to confirm the rmtree fix: all 24
  `genre.json`/`scenes.json`/`README.md` sidecars preserved, `git
  status` showed zero diff on `source/trial/`.
- `scripts/format --check --diff`, `scripts/lint` — clean.
- Editable-install-pointing-at-a-different-worktree issue recurred
  again and was fixed before trusting test output.
- `scripts/test tests/analysis_tests/annotate_test.py` — 14 tests pass
  (including the new `input_tokens`/`output_tokens` assertion).
- `scripts/test` (full suite) — 1652 tests pass.
- `lrh validate` — 0 errors.
- Re-ran `lcats promote`'s `survey_collection` directly against the
  trial output after the `annotate.py` schema change: still
  `clean=True`, 0 sidecar findings, `story_count=24` — confirms adding
  `input_tokens`/`output_tokens` to `scenes.json` didn't break the
  promote-gate validator's shape checks.
- Verified `text_segmenter.py`'s root cause directly: ran
  `text_segmenter.paragraph_text_indexer` against each affected
  story's real `body` text and confirmed `n_paragraphs=1` for all 3;
  verified the overlapping/degenerate `start_char`/`end_char` values
  against the real story text at those offsets (not just trusting the
  JSON) for `springtime_a_la_carte`, `snow_white_and_rose_red`, and
  `story_of_keesh` before writing up the finding.
- Systematic scan of all 24 stories' `scenes.json` for offset overlap
  and paragraph collapse (not just the 3 the bot flagged) — added as
  `collect_stats.py`'s `_segment_overlap_chars`/`_paragraph_collapse`
  functions, re-run after each edit to confirm the reported numbers
  (2 overlapping, 1 degenerate, 3/24 total) are exactly reproducible
  from the committed data, not hand-counted.
- `scripts/test`/`lrh validate` re-run after the `collect_stats.py`
  additions (see commit `0330ff1e`) — 1652 tests pass, 0 errors.

# Follow-up

Two follow-up work items recommended (documented in `stats_report.md`
and the primary execution record), neither implemented in this PR:

1. Output sanitization for the `secondary_genre` corruption
   (`assess.py`'s genre-detection call).
2. Fix `text_segmenter.py`'s `build_paragraph_index`/`align_segment`:
   handle single-newline-formatted source text, and make a failed
   anchor search raise an alignment failure instead of silently
   falling back to a full-range span — this is the more consequential
   of the two follow-ups, since it affects segmentation correctness
   for any shared caller of this library code, not just this item's
   own trial output.

This PR's own `input_tokens`/`output_tokens` fix (annotate.py) already
closes the separate `scene_analysis.py` cost-visibility gap the
primary execution record flagged, so that one is resolved, not a
follow-up.
